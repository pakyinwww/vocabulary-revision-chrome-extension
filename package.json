{
  "name": "word-notebook-chrome-extension",
  "version": "1.1.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build:extension": "node scripts/bundle-extension.js",
    "build:extension:no-test": "node scripts/bundle-extension-no-test.js",
    "pack:extension": "npm run build:extension && node scripts/pack-extension.js",
    "pack:extension:no-test": "npm run build:extension:no-test && node scripts/pack-extension.js",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "latest"
  },
  "packageManager": "npm@10.2.4"
}